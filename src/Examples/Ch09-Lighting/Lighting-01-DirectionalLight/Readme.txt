This example demonstrates basic directional (or infinite) lighting applied 
to a 3x3 grid of spheres.  The light direction may be rotated via keys. The
lighting is done per-vertex in a vertex shader.

The key commands are:

u, o - rotate light around up axis
Space - reset transforms
