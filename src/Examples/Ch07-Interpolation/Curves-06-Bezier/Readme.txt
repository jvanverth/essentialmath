This code example shows interpolation between a set of four points using piecewise 
Bezier interpolation.  The orange curve shows the Bezier path, the red points the
interpolating points, the yellow points the approximating control points.

Controls
--------
ESC: quit
SPACE: reset/stop animation
